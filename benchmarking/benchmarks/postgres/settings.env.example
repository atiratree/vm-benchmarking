# this file will be prepended to each install and run script

NAME="pg-benchmark"

# in GiB
RAM=1

# 1 thread for 2 clients for each core database has
PROCESSORS=1


RUN_TIME=3600

ARCHITECTURE=x86_64 # ppc64le
CENTOS=7
