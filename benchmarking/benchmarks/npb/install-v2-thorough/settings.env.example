# this file will be prepended to each install and run script

# you can use this settings for long running benchmarks

# <benchmark> is one of (BT, SP, LU, FT, CG, MG, EP, UA, IS, DC)
#     and <class> is one of (S, W, A, B, C, D, E), except that classes C,
#     D, and E are not defined for DC and class E is not defined for IS and UA.


# three pseudo applications
BT=D
SP=D
LU=D

# Benchmarks for unstructured computation, parallel I/O, and data movement
UA=D

THREADS=10

# if CG=E use: FFLAGS="-fdefault-integer-8"
FFLAGS=""
