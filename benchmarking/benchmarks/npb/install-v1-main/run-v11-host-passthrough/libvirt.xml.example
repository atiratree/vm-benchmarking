<!-- merge this with your base vm xml and put it into ./libvirt.xml -->
<domain>
  <cpu mode='host-passthrough'  match='exact' check='full'>
    <!-- if supported by underlying architecture -->
    <cache mode='passthrough'/>
  </cpu>
</domain>
